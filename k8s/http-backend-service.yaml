apiVersion: v1
kind: Service
metadata:
  name: syncpad-http-backend-service
  namespace: syncpad
spec:
  type: NodePort
  selector:
    app: syncpad-http-backend
  ports:
  - port: 4001
    targetPort: 4001
    protocol: TCP
    nodePort: 32001