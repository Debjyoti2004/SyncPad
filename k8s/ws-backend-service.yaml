apiVersion: v1
kind: Service
metadata:
  name: syncpad-ws-backend-service
  namespace: syncpad
spec:
  type: NodePort
  selector:
    app: syncpad-ws-backend
  ports:
  - port: 4002
    targetPort: 4002
    protocol: TCP
    nodePort: 32002