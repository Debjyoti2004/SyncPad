apiVersion: v1
kind: Service
metadata:
  name: syncpad-frontend-service
  namespace: syncpad
  labels:
    app: syncpad-frontend
spec:
  type: NodePort
  selector:
    app: syncpad-frontend
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      nodePort: 32000 